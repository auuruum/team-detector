[project]
name = "team-detector"
version = "0.1.0"
description = "Team detection program for games on BattleMetrics and Steam"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "networkx==3.3",
    "pyvis==0.3.2",
    "requests==2.31.0",
]
